server:
  port: 8081

spring:
  application:
    name: user-microservice
  datasource:
    password: postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://eshop-user-db/postgres
  flyway:
    url: jdbc:postgresql://eshop-user-db/postgres
    user: postgres
    password: postgres