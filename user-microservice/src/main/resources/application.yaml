server:
  port: 8081

spring:
  application:
    name: user-microservice
  datasource:
    password: postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://database/postgres
  flyway:
    url: jdbc:postgresql://database/postgres
    user: postgres
    password: postgres
  
  jpa:
    show-sql: true